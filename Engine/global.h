#ifndef ASCII_GLOBAL_INCLUDE
#define ASCII_GLOBAL_INCLUDE

// works for most bash like shells
#define WINDOW_CLEAR_COMMAND system("clear")

enum Entity_Type
{
    Entity_WINDOW,
};

#endif // ASCII_GLOBAL_INCLUDE